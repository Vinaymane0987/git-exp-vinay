// this file will contain add , sub , mul and div functions.
/**
 * adds to numbers and returns the results.
 * @param {number} a 
 * @param {number} b 
 * @returns the result
 */
const add = (a , b) => {
    return a + b;
}
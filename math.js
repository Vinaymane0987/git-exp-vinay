// this file will contain add , sub , mul and div functions.

/**
 * substract two numbers and returns the result
 * @param {number} a 
 * @param {number} b 
 * @returns the result
 */
const sub = (a , b) => {
    return a - b;
}